'use client';

import { useEffect, useState } from 'react';

export default function MapChart() {
  // GRS80TM > WGS84
  const [lo, setLo] = useState();

  useEffect(() => {}, []);

  return <div>맵차트 ui</div>;
}
